kind = "web"
previewPath = "/"
title = "LyricLingo"
version = "1.0.0"
id = "artifacts/lyriclingo"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20335

[services.development]
run = "pnpm --filter @workspace/lyriclingo run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/lyriclingo", "run", "build" ]
publicDir = "artifacts/lyriclingo/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20335"
BASE_PATH = "/"
