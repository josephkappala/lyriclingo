Make TWO frontend-only changes to my existing LyricLingo app. Stack: Node/Express backend serving a vanilla-JS frontend (public/index.html, style.css, app.js). Do NOT use React or Tailwind — plain HTML/CSS matching the existing files. Keep ALL current functionality intact: search, lyric view, guided playback (Play All), and the 1x/0.7x speed toggle must all keep working unchanged.
CHANGE 1 — Redesign the hero section only. Centered layout. Headline in the existing Fraunces serif: line 1 "Hear It. Read It. Speak It." then a lighter-gray subheading "Any song, two languages side by side — tap a line to hear it." Below it, make the search input a floating card: rounded (border-radius 20px), generous padding, soft drop shadow (box-shadow: 0 20px 60px rgba(0,0,0,0.25)), max-width ~640px, centered. Inside the card: the search input at top (transparent, no inner border, ~18px font) with placeholder Try "Despacito", "BTS", "Stromae"…; and a footer row with the existing "Translate to" dropdown styled as a small pill on the bottom-left and a circular search icon button on the bottom-right. All controls live INSIDE the card. CRITICAL: keep the card on the existing DARK theme with amber (#ffb454) accents — do NOT make it a white card or change the color scheme; it must visually match the lyric view. The search button and Enter key must call the EXISTING search function and run a real search (never console.log). The dropdown must stay wired to the existing translate logic. Keep the existing "TRY ONE OF THESE" example chips below the card.
CHANGE 2 — Change the search results from a list to a responsive grid of cards, album-grid style: each card shows the album cover art on top (square), the track title below, and the artist beneath in lighter text. If the track's explicit flag is set, show a small "E" badge on the card. Use the album cover art, title, artist, and explicit fields already returned by the existing track.search response — do NOT add new API calls or change the search route. Responsive: multiple columns on desktop, fewer on mobile. Cards with no cover art must show a clean placeholder, not a broken image. Tapping a card must open the lyric view exactly as it does now.
Do NOT modify any backend route, the lyric view, guided playback, or the speed toggle. These are styling/layout changes plus reuse of the existing search and translate functions only.